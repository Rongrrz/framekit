export const Enum = Object.freeze({
  LineJoinMode: Object.freeze({
    Miter: 'miter',
    Round: 'round',
    Bevel: 'bevel',
  }),

  BorderStrokePosition: Object.freeze({
    Inner: 'Inner',
    Outer: 'Outer',
    Center: 'Center',
  }),
});
